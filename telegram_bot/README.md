# Telegram Bot с GigaChat - Создание саммари сообщений

Telegram бот с интеграцией GigaChat API для создания саммари (выжимок) из сообщений, собранных скриптом Telethon.

## 🎯 Основной функционал

Бот создает структурированные саммари из новых сообщений, сохраненных в базе данных:
- Получает все новые сообщения из всех чатов (где `is_summarised = 0`)
- Объединяет информацию из разных чатов
- Создает общую выжимку с основными темами и событиями через GigaChat API
- Помечает обработанные сообщения в базе данных

## 📋 Установка

1. Установите зависимости:
```bash
pip install -r ../requirements.txt
```

2. Настройте переменные окружения:

### Для Telegram бота:
Создайте файл `.env` в директории `telegram_bot/` и добавьте токен бота:
```
BOT_TOKEN=your_bot_token_here
```

### Для GigaChat API:
Создайте файл `.env` в директории `ai/` и добавьте учетные данные GigaChat:
```
CLIENT_ID=your_client_id_here
CLIENT_SECRET=your_client_secret_here
```

**Важно:** `CLIENT_SECRET` должен быть в формате `Basic <base64_key>` или просто `<base64_key>` (префикс "Basic " будет добавлен автоматически).

## 🔑 Получение токенов

### Токен Telegram бота:
1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания нового бота
4. Скопируйте полученный токен и вставьте в файл `telegram_bot/.env`

### Учетные данные GigaChat:
1. Зарегистрируйтесь на [https://developers.sber.ru/gigachat](https://developers.sber.ru/gigachat)
2. Создайте приложение и получите `CLIENT_ID` и `CLIENT_SECRET`
3. Добавьте их в файл `ai/.env`

## 🚀 Запуск

```bash
cd telegram_bot
python bot.py
```

Бот будет работать до тех пор, пока вы не остановите его (Ctrl+C).

## 💬 Команды бота

### `/start`
Показывает приветствие и список доступных команд.

### `/summary` или `/саммари`
Создает саммари из новых сообщений:
- Получает все сообщения из базы данных, где `is_summarised = 0`
- Объединяет сообщения из всех чатов
- Отправляет в GigaChat API для создания выжимки
- Помечает обработанные сообщения
- Отправляет структурированное саммари пользователю

**Пример ответа:**
```
📊 Саммари новых сообщений

Обработано сообщений: 15

[Структурированная выжимка с основными темами и событиями из всех чатов]
```

### `/help` или `/помощь`
Показывает справку по командам.

## 🔄 Как это работает

1. **Сбор сообщений**: Скрипт Telethon (`telethon/main.py`) собирает сообщения из ваших чатов и сохраняет их в базу данных `telegram_messages.db`

2. **Запрос саммари**: Пользователь отправляет команду `/summary` боту

3. **Обработка**: 
   - Бот получает все новые сообщения (где `is_summarised = 0`)
   - Форматирует их для отправки в GigaChat
   - Создает запрос к GigaChat API с инструкцией объединить информацию из всех чатов

4. **Создание саммари**: GigaChat анализирует все сообщения и создает структурированную выжимку

5. **Обновление статуса**: Все обработанные сообщения помечаются как `is_summarised = 1`

6. **Отправка результата**: Бот отправляет саммари пользователю

## 📁 Структура

- `bot.py` - основной файл бота с обработчиками команд
- `summary.py` - модуль для создания саммари через GigaChat
- `db_utils.py` - утилиты для работы с базой данных
- `.env` - файл с токеном бота (не коммитится в git)
- `bot.log` - файл с логами работы бота

## ⚙️ Особенности

- **Только новые сообщения**: Саммари создается только из сообщений, которые еще не были обработаны
- **Объединение из всех чатов**: Информация из разных чатов объединяется в одну общую выжимку
- **Автоматическое отслеживание**: Статус обработки сообщений отслеживается автоматически
- **Обработка длинных ответов**: Если саммари слишком длинное, бот разбивает его на несколько сообщений
- **Логирование**: Все действия бота записываются в файл `bot.log` и выводятся в консоль

## 🔍 Требования

- База данных `telegram_messages.db` должна существовать (создается скриптом Telethon)
- Таблица `messages` должна содержать колонку `is_summarised`
- Настроенные учетные данные GigaChat API

## 📝 Примечания

- Бот работает только с командами - обычные текстовые сообщения показывают список команд
- Саммари создается по запросу, не в фоне
- Для работы бота необходимо, чтобы скрипт Telethon собирал сообщения в базу данных
