# Telegram messages summarizer - Project with Telegram and GigaChat

Проект для работы с Telegram через Telethon, генерации выжимок текста через GigaChat API и создания дашборда для просмотра статистики сообщений.

## 🚀 Основные возможности

- **Сбор сообщений из Telegram**: Автоматический сбор сообщений из всех чатов и каналов через Telethon
- **База данных SQLite**: Хранение всех сообщений с отслеживанием статуса обработки
- **Создание саммари**: Генерация выжимок из новых сообщений через GigaChat API
- **Telegram бот**: Бот для создания саммари по команде `/summary`
- **Web дашборд**: Flask приложение для просмотра статистики и списка сообщений

## 📁 Структура проекта

```
/project
├── telethon/              # Модуль для работы с Telegram
│   ├── main.py           # Основной скрипт Telethon (сбор сообщений)
│   ├── config.py         # Конфигурация Telegram API
│   ├── db.py             # Работа с базой данных SQLite
│   └── test_db.py        # Тестовый скрипт для проверки БД
│
├── telegram_bot/         # Telegram бот с функционалом суммаризации
│   ├── bot.py            # Основной файл бота
│   ├── summary.py        # Модуль для создания саммари
│   ├── db_utils.py       # Утилиты для работы с БД
│   ├── .env              # Токен бота (создайте сами)
│   └── README.md         # Документация по использованию бота
│
├── ai/                   # Модуль для работы с GigaChat API
│   ├── main.py          # CLI-приложение для генерации выжимок
│   ├── gigachat.py      # Модуль работы с GigaChat API
│   ├── utils.py         # Вспомогательные функции
│   ├── .env             # Учетные данные GigaChat (создайте сами)
│   └── README.md        # Документация по использованию GigaChat CLI
│
├── flask/                # Web дашборд для просмотра статистики
│   ├── app.py           # Flask приложение
│   ├── flask_db.py      # Модуль для работы с БД
│   ├── templates/       # HTML шаблоны
│   ├── static/          # CSS стили
│   ├── requirements.txt # Зависимости Flask приложения
│   └── README.md        # Документация по дашборду
│
├── telegram_messages.db  # База данных SQLite (создается автоматически)
├── requirements.txt      # Зависимости проекта
└── README.md            # Этот файл
```

## 🔧 Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd Intensive_GigaChat_TG
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Для Flask дашборда установите дополнительные зависимости:
```bash
cd flask
pip install -r requirements.txt
cd ..
```

## ⚙️ Настройка

### Для Telethon (сбор сообщений):

Отредактируйте `telethon/config.py` и укажите ваши `API_ID` и `API_HASH` из https://my.telegram.org/apps

### Для GigaChat API:

Создайте файл `ai/.env` и добавьте:
```
CLIENT_ID=your_client_id_here
CLIENT_SECRET=your_client_secret_here
```

Учетные данные можно получить на https://developers.sber.ru/gigachat

**Важно:** `CLIENT_SECRET` должен быть в формате `Basic <base64_key>` или просто `<base64_key>` (префикс "Basic " будет добавлен автоматически).

### Для Telegram бота:

Создайте файл `telegram_bot/.env` и добавьте:
```
BOT_TOKEN=your_bot_token_here
```

Токен бота можно получить у [@BotFather](https://t.me/BotFather) в Telegram

## 📖 Использование

### 1. Сбор сообщений из Telegram (Telethon)

Запуск скрипта для сбора сообщений:
```bash
cd telethon
python main.py
```

Скрипт:
- Подключается к Telegram
- Получает список всех чатов
- Собирает последние 100 сообщений из первого чата
- Запускает live-слушатель новых сообщений
- Сохраняет все сообщения в базу данных `telegram_messages.db`

### 2. Telegram бот (создание саммари)

Запуск бота:
```bash
cd telegram_bot
python bot.py
```

**Команды бота:**
- `/start` - Приветствие и список команд
- `/summary` или `/саммари` - Создать саммари из новых сообщений
- `/help` или `/помощь` - Справка

**Как работает саммари:**
1. Бот получает все новые сообщения из базы данных (где `is_summarised = 0`)
2. Объединяет сообщения из всех чатов
3. Отправляет в GigaChat API для создания общей выжимки
4. Помечает обработанные сообщения как `is_summarised = 1`
5. Отправляет структурированное саммари пользователю

Подробнее см. `telegram_bot/README.md`

### 3. GigaChat CLI (генерация выжимок)

Генерация выжимки текста:
```bash
cd ai
python main.py summary --file messages.txt
# или
python main.py summary --text "Ваш текст"
```

Подробнее см. `ai/README.md`

### 4. Web дашборд (Flask)

Запуск дашборда:
```bash
cd flask
python app.py
```

Приложение будет доступно по адресу: http://localhost:5000

**Страницы:**
- `/` - Статистика (количество сообщений, каналов, чатов, обработанных сообщений)
- `/messages` - Список всех сообщений с датой/временем в MSK и статусом обработки

Подробнее см. `flask/README.md`

## 🗄️ База данных

Проект использует SQLite базу данных `telegram_messages.db` со следующей структурой:

**Таблица `messages`:**
- `id` - ID сообщения в Telegram
- `chat_id` - ID чата
- `sender` - Имя отправителя
- `type` - Тип чата (Channel/Chat)
- `text` - Текст сообщения
- `date` - Дата и время сообщения
- `is_summarised` - Статус обработки (0 - не обработано, 1 - обработано)
- PRIMARY KEY: `(id, chat_id)`

**Таблица `summary_state`:**
- Отслеживание последнего обработанного сообщения (для обратной совместимости)

## 📦 Зависимости

Основные зависимости (корневой `requirements.txt`):
- `telethon>=1.34.0` - для работы с Telegram через Telethon
- `requests>=2.31.0` - для HTTP-запросов к GigaChat API
- `python-dotenv>=1.0.0` - для работы с переменными окружения
- `pyTelegramBotAPI>=4.14.0` - для создания Telegram бота

Дополнительные зависимости для Flask (`flask/requirements.txt`):
- `Flask==3.0.0` - веб-фреймворк
- `pytz==2023.3` - работа с часовыми поясами

## 🔒 Безопасность

⚠️ **Важно:** Не коммитьте файлы с конфиденциальными данными:
- `.env` файлы
- `telegram_session.session` файлы
- `telegram_messages.db` (если содержит личные данные)

Добавьте в `.gitignore`:
```
*.env
*.session
*.session-journal
*.db
__pycache__/
*.pyc
*.log
venv/
```

## 🐍 Требования

- Python 3.10+

## 📝 Лицензия

[Укажите вашу лицензию]

## 🤝 Вклад в проект

Приветствуются любые улучшения и предложения!
